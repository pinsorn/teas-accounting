// Sprint 13j-FE — ListFilters is now the 5-column grid `FilterBar`.
// Kept as a named re-export so the existing 8 list-page call sites
// (`import { ListFilters } from '@/components/ui/ListFilters'`) keep working.
export { FilterBar as ListFilters } from '@/components/ui/FilterBar';
