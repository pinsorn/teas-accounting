> ## ✅ RESOLVED 2026-05-19 — DO NOT ACTION
> Prior-session relay supplied the fix: the long MSIX path broke MSBuild node
> launch (Win32 87). **Workaround: `subst U:` short-path drive, run `dotnet`
> via the PowerShell tool from `U:\backend` (plain `dotnet`, no env vars,
> Bash only for git).** Verified this session: `dotnet build Accounting.sln`
> → **0 errors / 0 warnings**; `dotnet test Accounting.Domain.Tests` →
> **89/89 pass**. P3 backend is now **VERIFIED** (not BUILD-PENDING).
> Q1/Q2 are moot — proceeding P2→P4→P5 with full in-harness build + test +
> `dotnet ef`. No Sana/Ham action needed. Kept for audit trail only.

---

# Question-Backend14 — Sprint 13e: .NET toolchain dead in this session env (BEFORE P2/P4 backend)

**Date:** 2026-05-19
**From:** Claude Code
**To:** Ham / Sana
**Re:** Sprint 13e resume (P3→P2→P4→P5). P3 shipped + FE-verified. P2/P4 have
substantial backend incl. a **breaking EF migration** that **cannot be built
or verified in this session's environment**. Same spec-first discipline as
Question-Backend5/12/13 — surfacing the blocker BEFORE writing unverifiable
backend code. Recommended path included for a fast yes/adjust.

> Nothing backend-substantive written for P2/P4 yet. P3's tiny BE delta is
> done + code-reviewed (build env-blocked, see below).

---

## The blocker (hard environment fact, not a code error)

Every .NET compile path fails at OS process-creation:

```
Microsoft.Build.BackEnd.NodeFailedToLaunchException: The parameter is incorrect.
   at Microsoft.Build.BackEnd.NodeLauncher.StartProcessWindows(...)
MSB1025: Cannot acquire required number of nodes. NumberOfNodesToCreate: 2
```
and, when MSBuild got one step further:
```
error MSB3883: An error occurred trying to start process
'...\Roslyn\bincore\csc.exe' ... The parameter is incorrect.
```

Reproduced across **all** of: `dotnet build` via git-bash (sandboxed),
git-bash with `dangerouslyDisableSandbox`, PowerShell with absolute path,
`-m:1 -nodeReuse:false -p:UseSharedCompilation=false --disable-build-servers`,
`DOTNET_CLI_USE_MSBUILD_SERVER=0`, `MSBUILDDISABLENODEREUSE=1`.

Conclusion: this Claude session (packaged MSIX `Claude_pzs8sxrjxfjjc`) cannot
spawn MSBuild worker nodes **or** `csc.exe` — the entire C# compile toolchain
is unusable here. **`dotnet ef migrations add` will fail identically** (it
builds first). Node-based tooling is fine: frontend `tsc --noEmit` runs clean.

This means for Sprint 13e:
- **P3** — BE delta was 2 optional record params + 2 EF `Where` clauses;
  code-reviewed correct; FE fully verified (`tsc` 0). Low risk. **Shipped.**
- **P2** — needs `Quotation` model fields + status enum + transition
  endpoints + a **breaking `AddQuotationWorkflowFields` EF migration**
  (backfill existing stub Q rows). Migration **cannot be generated** here
  (`dotnet ef` builds first) and BE **cannot be compiled/tested**.
- **P4** — SO/DO controllers + status transitions + Q→SO convert. Same
  problem (no build, no test).
- **P5** — pure frontend (`DocumentStatusBadge`), verifiable via `tsc`. Fine.

---

## ✅ P3 — done this session (for context)

- **BE** `GET /tax-invoices`: added `Search` (doc_no / customer name ILIKE) +
  `Unpaid` (`AmountPaid < TotalAmount`) filters. Files: `TaxInvoiceDtos.cs`
  (`TaxInvoiceListQuery` +2 trailing optional params — backward compatible),
  `TaxInvoiceEndpoints.cs` (`TaxInvoiceListQueryParams` + constructor call),
  `TaxInvoiceService.Read.cs` (2 `Where` clauses; `EF.Functions.ILike`,
  Npgsql, namespace already imported). Code-reviewed; **build env-blocked**.
- **FE** new `frontend/components/forms/TaxInvoicePicker.tsx` (async combobox,
  mirrors `CustomerSelector`: doc_no/customer search, preview row
  `doc_no · customer · date · total`, `customerId`/`status`/`unpaid` scoping,
  disabled-until-customer for RC). Wired into `/receipts/new` (per-row,
  customer-scoped, unpaid, auto-fills appliedAmount = TI total) and
  `AdjustmentNoteForm` (CN/DN, `status="Posted"`). **`tsc --noEmit` → 0.**

---

## 🟡 Q1 — How should P2/P4 backend proceed given no build/verify here?

**Recommended R-Q1a (momentum + honesty — recommended):**
"FE-now, BE-code + BUILD-PENDING handoff." This session:
- Build P2 + P4 + P5 **frontend** fully (ProductPicker, LineItemsTable,
  QuotationForm, SalesOrderForm, DeliveryOrderForm, DocumentStatusBadge,
  rebuilt pages) — all verifiable via `tsc`.
- Write the P2/P4 **backend** code (Quotation/SO/DO model fields, status
  enums, transition endpoints) **and** the EF migration **by hand** following
  the existing `20260517180740_AddQuotationChain` migration as the template,
  clearly marked `// BUILD-PENDING: generated by hand — CI/Sana must
  `dotnet ef migrations add`-regenerate or compile-verify before merge`.
- Report-Backend29 flags every BE file as BUILD-PENDING; Sana/CI does the
  authoritative compile + `dotnet test` + migration apply before merge.
- Consistent with the project's accepted "degrade cleanly, defer the
  authoritative check to the channel that can do it" pattern (8.6 R-B1a;
  P1 client-render → Sana Chrome-MCP acceptance).

**R-Q1b (conservative):** Ship **P3 only** + P5 (both Node-verifiable);
**defer P2/P4 entirely** to a session/CI with a working .NET toolchain.
Zero unverified backend enters the tree. Lowest risk, slowest.

**R-Q1c (aggressive):** Write all P2–P5 code now, accept BE unbuilt/untested
this session. Highest risk of compile errors slipping into a breaking
migration — the exact "half-finished" anti-pattern Report-Backend26/28
warned against. **Not recommended.**

I lean **R-Q1a**: it keeps the sprint moving, the hand-written migration is a
mechanical mirror of an existing one (low novelty), and the BUILD-PENDING
marking is honest and matches prior accepted handoff patterns. But the
**breaking migration on existing Q rows** is compliance-adjacent enough that
if you want zero unverified BE in the tree, **R-Q1b** is the safe call —
your decision.

## 🟡 Q2 — Is the env fixable on your side?

If you (Ham) can run this session with a host that permits child-process
creation (or if Sana's environment can build), R-Q1a's handoff is trivial and
P2/P4 BE gets verified same-day. Otherwise assume the toolchain stays dead
here for the rest of Sprint 13e and plan around R-Q1a/R-Q1b.

---

## Ask

One line back: **R-Q1a / R-Q1b / R-Q1c**, and whether Q2's env fix is
possible. I proceed immediately on your pick. If no answer this session, I
default to **R-Q1a** for FE + P5 (safe, Node-verifiable) and hold the P2/P4
**backend** writes until you confirm (avoids an unverifiable breaking
migration landing without sign-off).
