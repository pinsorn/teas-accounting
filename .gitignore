# ===== .NET =====
bin/
obj/
out/
publish/
*.user
*.suo
*.userosscache
*.sln.docstates
.vs/
*.dll
*.pdb

# ===== Node / Next.js =====
node_modules/
.next/
.turbo/
out/
*.tsbuildinfo
next-env.d.ts
.swc/

# ===== Env / secrets =====
.env
.env.local
.env.*.local
*.pfx
*.key
*.pem
/secrets/
# First-run instance secrets (MFA AES key, JWT lifetime) written by the onboarding
# setup endpoint — NEVER commit. Stays a config KEY in appsettings*; the VALUE lives here.
appsettings.Secrets.json
**/appsettings.Secrets.json

# ===== IDE =====
.idea/
.vscode/
*.code-workspace
.DS_Store
Thumbs.db

# ===== Test / Coverage =====
TestResults/
coverage/
*.coverage
*.coveragexml

# ===== Logs =====
logs/
*.log

# ===== Local DB / Docker volumes =====
pgdata/
redisdata/

# ===== Build artifacts =====
dist/
build/
*.tgz
*.zip

# ===== OS =====
*.swp
*.swo

# ===== Playwright =====
playwright-report/
test-results/
# e2e run artifacts (results JSON regenerated by rbac-ui-gating.spec → manual generator)
frontend/e2e/.artifacts/

# ===== Outputs / generated =====
schema.dump.sql

# ===== Secrets (Sprint 13c — dev signing certs; NEVER commit) =====
secrets/
**/secrets/
*.pfx
*.pem

# ===== Runtime file-storage (Sprint 11 LocalDisk; never commit) =====
_attachments/

# Derived knowledge-graph artifacts (CLAUDE.md §17 — regenerate, never commit)
graphify-out/

# Windows reserved-name stray file (blocks git add -A; cannot be stat'd)
/nul

# Transient API stderr logs
*.err

# Playwright MCP browser-test scratch output (snapshots/screenshots)
.playwright-mcp/

# cont.80 — create-form redesign reference (never commit)
frontend/public/_mockup.html
mockup-*.png
pilot-*.png
rollout-*.png

# ===== Local review artifacts + RD/SSO source binaries =====
# Ham 2026-06-12: link the official RD URLs; never commit the ~60MB source PDFs/rasters.
# (Embedded filler templates under backend/src/**/Pdf/Templates are committed deliberately.)
_review/
docs/RD-Forms/**/*.pdf
docs/RD-Forms/**/*.png
docs/SSO-Forms/**/*.pdf
