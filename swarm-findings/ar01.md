# ar01 (AR Clerk) — UX swarm findings — co5 prod (teas.kazaki-rio.com v1.22.5)

Run: 2026-07-19 ~17:5x-18:1x GMT+7 | Target: https://teas.kazaki-rio.com | User: ar01 / role AR Clerk
Tool: Playwright headless (msedge channel) from frontend/, temp scripts (swarm-ar01*.mjs), deleted after run.

## Done

- Logged in as ar01; dashboard confirms co5 context (บริษัท ทดสอบ VAT (DUMMY) จำกัด). No other-company data seen anywhere in this session — no tenant-leak observed.
- Attempted TI ตรง for customer C002 (นายสมชาย ใจดี, บุคคล) twice (draft #4 total 5,350, draft #5 total 1,070): draft creation succeeded both times (201), **posting failed both times with HTTP 500** — see CRIT-1. Both docs are stuck as orphaned Drafts (#4, #5) in the TI list — cleanup candidate for consolidation.
- Fell back to the pre-existing posted TI `07-2026-TI-0002` (customer C002, unpaid, ฿5,350.00) to continue the RC/CN chain:
  - Issued a Receipt against it (customer picked C002, applied ฿5,350 full settle, submitted) — **post also failed with HTTP 500** (`POST /api/proxy/receipts/9/post`) — see CRIT-2. Same failure shape as the TI: draft created, post 500s.
  - Created a Credit Note **draft** against TI-0002 via the dedicated "บันทึก" (save-draft) button — succeeded, landed correctly on `/credit-notes` list. Confirms CN drafting itself works and correctly requires a linked original TI (the very first CN attempt, run before any posted TI existed in this session, correctly blocked submission client-side with a red "ระบุใบกำกับภาษีเดิม" validation message rather than a silent failure).
  - CN post-permission probe (SoD: can AR Clerk post a CN?) was attempted but **not conclusively completed** — a selector issue in my follow-up script failed to reliably reopen the freshly-created draft's detail page a second time. Not re-attempted further given the timebox; flagging as an open item for consolidation/re-verification rather than reporting an unverified result.
- Viewed AR aging report (`/reports/ar-aging`): reconciliation ("tie") banner reads **บัญชีคุมยอด (1130) ฿4,280.00 = ยอดรวมทะเบียนย่อย ฿4,280.00, Dr = Cr ✓** — control account and subledger total tie correctly. Verified this reflects only the pre-existing posted baseline docs (my failed TI drafts #4/#5 never posted, so correctly excluded — checked their detail pages and the TI list to confirm both remain `Draft`, ruling out a silent partial-commit).
- Probed RBAC boundaries via direct URL nav (not via any visible nav link) for `/purchase-orders/new`, `/vendor-invoices/new`, `/payment-vouchers/new`, `/period-close`, `/reports/pnd30` — see HIGH findings below. Did not submit any PO/VI/PV form and did not click any close-period/confirm-ภ.พ.30 control, per hard rule 2.
- Console/network error capture ran for the whole session (see CRIT-1/2 evidence).

## Findings

| Severity | Area | Symptom | Repro | Screenshot |
|---|---|---|---|---|
| CRIT | Tax Invoice post (`POST /api/proxy/tax-invoices/{id}/post`) | Posting a freshly-created TI draft for customer C002 fails with a generic 500 (`{"type":"urn:teas:error:internal_error","title":"internal_error","status":500,"detail":"An unexpected error occurred."}`). Reproduced twice in a row in two separate sessions (id 4: 2x2,500=5,350 total; id 5: 1x1,000=1,070 total). Draft creation itself succeeds (201); only the post step 500s. Both docs left stranded as Draft — core AR-clerk task ("TI ตรง → post") cannot be completed for this customer as ar01 right now. Given 10 role-agents are hammering co5 concurrently, a doc-numbering/allocation race under load is the most likely cause, but this cannot be confirmed from the UI/client side alone — needs API/server-log correlation. | Login ar01 → `/tax-invoices/new` → pick customer C002 → 1 line item → Post → confirm → observe 500 on `/tax-invoices/{id}/post`; TI stays `Draft` in the list. | shots/ar01-02-ti-error.png, shots/ar01-10-ti-retry.png, shots/ar01-12-ti-4-detail.png, shots/ar01-12-ti-5-detail.png, shots/ar01-13-ti-list.png |
| CRIT | Receipt post (`POST /api/proxy/receipts/{id}/post`) | Same failure shape as CRIT-1 on a different document type: issuing a Receipt against posted TI `07-2026-TI-0002` (customer C002, ฿5,350 applied) — draft created, but posting 500s (`/receipts/9/post`). Two different "post" endpoints failing with a generic 500 in the same session raises the likelihood this is a shared posting-pipeline issue (possibly numbering/journal-entry allocation under concurrent swarm load) rather than one-off per-document logic. | Login ar01 → `/receipts/new` → customer C002 → apply to TI `07-2026-TI-0002`, amount 5350 → Post → confirm → 500 on `/receipts/9/post`. | shots/ar01-14-rc-error.png |
| HIGH | RBAC — period-close page | Direct URL nav to `/period-close` as AR Clerk renders the **full** period/year-end close management page: year selector, all 12 months with status (`เปิด`/open), and a "ปิดบัญชีสิ้นปี" (year-end close) section with a notes field — no redirect, no 403, no access-denied message. This is squarely the area HARD RULE 2 protects (period close / ภ.พ.30 confirm), and it should not be reachable by a clerk-level role at all, regardless of whether the actual close action is separately gated server-side. Did NOT click into any month row or the year-end-close control, per hard rule. | Login ar01 → type `/period-close` directly in the address bar. | shots/ar01-08-probe-period-close.png |
| HIGH | RBAC — PO/VI/PV create forms | Direct URL nav to `/purchase-orders/new`, `/vendor-invoices/new`, `/payment-vouchers/new` all render the **complete, functional** create form for AR Clerk (vendor picker, line items, live A4 preview, and an enabled "บันทึก"/Post submit button) — no redirect, no 403 page. Underlying data-fetch calls on these pages did return HTTP 403 (seen in the console/network capture), but that 403 is swallowed silently — the form still renders normally with no visible error/toast telling the user anything failed, so from the UI alone AR Clerk appears fully able to open and fill out AP/Purchasing documents. Did not click Post/submit on any of these (scope: probe only, avoid creating cross-role documents that could confuse other concurrent agents' PO/VI numbering). Whether the actual submit would be rejected server-side is unverified. | Login ar01 → type each URL directly; observe rendered form + silent 403s in network tab. | shots/ar01-05-probe-purchase-orders.png, shots/ar01-06-probe-vendor-invoices.png, shots/ar01-07-probe-payment-vouchers.png |
| LOW | ภ.พ.30 confirm button visibility | On `/reports/pnd30`, the "ยืนยัน/ปิดงวด" button is present in the DOM and technically visible for ar01, but its styling (light-gray fill) contrasts with the active orange "แสดงตัวอย่าง" button next to it, consistent with a `disabled` state — could not confirm the `disabled` attribute directly (did not attempt to click it, per hard rule 2). Recommend a quick follow-up (inspect the DOM attribute only, never click) to confirm it is truly non-interactive for this role rather than just visually muted. | Login ar01 → `/reports/pnd30` → select July 2026 → observe button style. | shots/ar01-09-probe-pnd30.png |
| LOW | Orphaned Draft documents | TI #4, #5 (this session) and Receipt #9 are stranded in Draft status as a side effect of CRIT-1/CRIT-2 above — not a bug by itself, just test litter co5 now carries. Flagging for the consolidation cleanup pass. | — | shots/ar01-13-ti-list.png |

## Denied-as-expected

- No RBAC deny actually fired cleanly for the PO/VI/PV/period-close probes above — see HIGH findings; none of them behaved as a "clean deny" (no redirect, no 403 page, no visible message). Recording this explicitly since the hard rules define clean-deny as the expected baseline and none of ar01's probes hit it.
- AR aging tie banner: balanced (Dr = Cr ✓) — not a deny probe, but confirms the "control account = subledger total" invariant holds under current swarm load, no discrepancy found.
