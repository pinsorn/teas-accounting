# ar01 (AR Clerk) — UX SWARM ROUND 3 findings — co5 prod (teas.kazaki-rio.com v1.22.6)

Run: 2026-07-19 ~23:4x-23:5x GMT+7 | Target: https://teas.kazaki-rio.com | User: ar01 / role AR Clerk
Tool: Playwright headless (msedge channel) from frontend/, temp script `swarm3-ar01.mjs` (deleted after run).
Two runs: run 1 hit tooling issues only (page.goto 'load'-event hang under swarm load, short waits) — no
findings drawn from it, script hardened (domcontentloaded nav, 25s waits, network-level capture of the
create + post calls, toast-text scrape) and re-run. **All results below are from run 2**, the clean run.

## Done

- Logged in as ar01; dashboard confirms co5 context (บริษัท ทดสอบ VAT (DUMMY) จำกัด, "ทดสอบ VAT"/"DUMMY"
  text present). No other-company text (นาย พงศ์สันต์ / เรปทาวน์) seen anywhere — no tenant-leak.
- Ran 3x full cycles of: `/tax-invoices/new` → pick customer C002 (นายสมชาย ใจดี) → 1 line item (qty 1,
  ฿1,000, VAT 7% → grand total ฿1,070) → click Post → confirm dialog → confirm post.
  - **All 3 cycles: draft creation succeeded (`POST /api/proxy/tax-invoices` → 201, ids 7/8/9), then
    posting failed (`POST /api/proxy/tax-invoices/{id}/post` → 500)**, every single time, byte-identical
    error body. See CRIT-1 finding below — this is the primary result of this round.
  - Because every TI post 500'd, no TI ever reached `Posted` status, so the planned RC(against posted
    TI)→post half of each cycle could not be exercised at all (there was nothing valid to receipt
    against) — RC post is **unverified this round**, blocked upstream by the TI-post 500. Did not
    fabricate a receipt against an unposted/stale doc to force a result.
- AR aging report (`/reports/ar-aging`) tie banner: **บัญชีคุมยอด (1130) ฿4,280.00 = ยอดรวมทะเบียนย่อย
  ฿4,280.00, Dr = Cr ✓** — ties correctly, and correctly excludes my 3 failed-to-post TI drafts (ids
  7/8/9 never left Draft, so they're absent from the aging register as expected — no silent partial
  commit). Same ฿4,280.00 figure as round 2, consistent with no new posted AR activity from this session.
- Console/network capture ran for the whole session; no cross-tenant data observed at any point.

## CRIT-verify (explicit)

- **CRIT-1 (numbering-write 500s): NOT CLOSED.** TI post is still hard-broken: 3/3 attempts (ids 7, 8, 9)
  returned `HTTP 500` from `POST /api/proxy/tax-invoices/{id}/post` with body
  `{"type":"urn:teas:error:internal_error","title":"internal_error","status":500,"detail":"An unexpected
  error occurred."}` — the exact same generic-500 shape as round 2's finding, now reproduced deterministically
  (3/3, not a one-off race). Draft creation (`POST /api/proxy/tax-invoices` → 201) works fine every time;
  only the post step fails. This is a **CRITICAL regression from round 2, still present in v1.22.6** — the
  626 seq-drift reconcile/retry-guard fix did not close this path for AR Clerk / customer C002.
  RC post could not be independently tested (blocked upstream, see above) — **RC-post 2xx status is
  UNKNOWN this round**, not confirmed passing.
- AR aging tie banner: confirmed still holds (Dr = Cr ✓, control account = subledger total) — this part of
  the ask is fine, unaffected by the TI-post failure.

## Findings

| Severity | Area | Symptom | Repro | Screenshot |
|---|---|---|---|---|
| CRIT | Tax Invoice post (`POST /api/proxy/tax-invoices/{id}/post`) | Posting a freshly-created TI draft for customer C002 fails with `HTTP 500 {"type":"urn:teas:error:internal_error","title":"internal_error","status":500,"detail":"An unexpected error occurred."}`, reproduced **3/3** times in this run (ids 7, 8, 9), all in a single session, human-paced (not a rapid loop) — matches round 2's exact failure shape, still present in v1.22.6. Draft creation itself succeeds (201); only the post step 500s every time. No toast/error message was visibly shown to the user in any of the 3 attempts (toast text scrape came back empty — either it auto-dismissed before capture, or none rendered; the form just silently re-enabled). **CRIT-1 is NOT closed.** | Login ar01 → `/tax-invoices/new` → pick customer C002 → 1 line item (qty 1, ฿1,000) → click "บันทึกเอกสาร (Post)" → confirm dialog → "ยืนยันบันทึก" → observe `POST /api/proxy/tax-invoices/{id}/post` → 500. Repeated 3x back-to-back (ids 7, 8, 9), same result every time. | shots/round3/ar01-03-c1-ti-post-FAILED.png, shots/round3/ar01-05-c2-ti-post-FAILED.png, shots/round3/ar01-07-c3-ti-post-FAILED.png (plus the filled-form state just before each failed post: ar01-02-c1-ti-filled.png, ar01-04-c2-ti-filled.png, ar01-06-c3-ti-filled.png) |
| HIGH | Receipt post — unverified | Because TI post never succeeds, no posted TI exists to receipt against, so RC creation/post could not be driven through the intended real flow (per the "exercise the real transition, don't fake the precondition" rule, no attempt was made to receipt against an unposted doc or a stale pre-existing TI just to force a green result). **RC post status this round: UNKNOWN**, not "passing" — flag for consolidation: needs a re-run once TI post is actually fixed, or a dedicated probe against an already-posted baseline TI if the swarm needs an RC-only read this round. | N/A — blocked upstream | — |
| LOW | Orphaned Draft documents | TI ids 7, 8, 9 (this session) are stranded as `Draft` in the TI list as a direct side effect of CRIT-1 — not a new bug, just test litter co5 now carries (same pattern as round 2's ids 4, 5). Cleanup candidate for consolidation. | — | — |

## Denied-as-expected

- N/A this round — ar01's mission was the numbering-write PRIMARY assertion + AR aging tie check, not RBAC
  probing (that was covered in round 2's ar01 pass). No deny-path probes were attempted this round to stay
  inside the ~25-min timebox and keep focus on CRIT-1 verification, which is this round's reason to exist.
