# Sprint 13j-PURCH — Bug Log

> Bugs found / fixed during the Purchase parity sprint. Subagents append here when they hit something; main agent triages.
> ID scheme: `BP-NN` (Bug Purchase). Severity: 🔴 blocker · 🟠 major · 🟡 minor · 🔵 polish.
> Known Sales bug classes referenced by spec: **#SR8** (used `window.print()` of HTML — must use `/pdf` endpoint), **#SR9** (generic "เกิดข้อผิดพลาด" toast — must give specific Thai message).

---

## Open

| ID | Sev | Phase | Area | Symptom | Repro / root cause | Status |
|---|---|---|---|---|---|---|
| BP-01 | 🟡 | A | Test `PurchaseAuditTests.Pv_post_with_wht_records_certificate_generated_issued` | One-off `DbUpdateException` on a single run (11/12); test is green otherwise | NOT reproduced in 6 subsequent full-suite runs (incl. the required 2× consecutive + a 4× loop). No inner exception captured. Suspected transient number-sequence/connection race on a freshly-migrated `teas_test` row; all unique seeds already use `TestIds.*` so not a hardcoded-collision bug. Gate A still PASSES (2× consecutive met, 6/6 total). | OPEN — watch; if it recurs, capture `ex.InnerException.Message` to identify the exact constraint |
| BP-02 | 🔵 | B | `docs/api/openapi.yaml` line ~1269 (PRE-EXISTING, not introduced this sprint) | Strict YAML parser (PyYAML `safe_load`) throws `ScannerError: mapping values are not allowed here` on the whole file | An unquoted `description:` scalar contains `Idempotency-Replayed: true` (a `: ` inside an unquoted block scalar). Present in HEAD before Phase B (confirmed via `git show HEAD`). My Phase B additions parse cleanly in isolation. Lenient OpenAPI tooling (Swagger/Redoc/openapi-validator) accepts it; only strict generic-YAML scanners trip. | OPEN — low priority; fix by quoting that description string if a strict YAML lint is ever wired into CI |
| BP-04 | 🟠 | D | `vendor-invoices/[id]` print | No way to print/PDF a Vendor Invoice from the detail page | There is NO `GET /vendor-invoices/{id}/pdf` endpoint in `VendorInvoiceEndpoints.cs` (Phase C added PDF+`?copy`+`mark-printed` only for PO/PV). So D3 could NOT wire a PrintMenu to VI — VI detail has no print control. If VI needs a printable artifact, BE must add a `/pdf` (and optionally tracking) endpoint. | OPEN — needs BE decision (was VI meant to be printable? It's a *received* vendor doc) |
| BP-05 | 🟠 | D | `DocumentCrossRefService.GetChainAsync` + `DocumentChainDto` (BE) | `GET /documents/chain` returns null for Purchase anchors; no server-resolved `<DocumentChain>` rail on PO/VI/PV/WHT | The unified chain resolver `switch(anchorType)` only handles Sales anchors and `default: return null`. `DocumentChainDto` is a fixed 7-slot Sales shape. Extending to Purchase = NEW DTO + NEW walk + parallel FE renderer. **MITIGATED Phase 1:** FE `PurchaseDocumentChain` renders the chain from upward cross-refs already on each detail DTO (+ PO→firstVI). Full bi-directional server chain deferred. | **RESOLVED (Flag-2, subAgent8)** — added downward refs `VendorInvoiceDetail.settlingPvs[]` (PV that settle the VI) + `PaymentVoucherDetail.whtCertificates[]` (50ทวิ from the PV) on the read DTOs; `PurchaseDocumentChain` now resolves BOTH directions (VI→PV, PV→WHT, and PO→VI→PV→WHT). No new endpoint/entity/migration. BE 0/0, FE tsc 0, next build 0/0. |
| BP-07 | 🟡 | G | Test `Sprint9VatComplianceTests.Pnd30_finalize_is_immutable` (PRE-EXISTING Sales/VAT test, NOT touched this sprint) | Full `Accounting.Api.Tests` suite: run 1 = **174/174 green**; run 2 consecutive (same `teas_test`) = **173/174**, this one test fails | NOT a sprint regression: (a) it is Sales-VAT code untouched by Purchase work; (b) `Sprint9VatComplianceTests` passes **5/5 ×2 in isolation**; (c) full-suite run 1 was clean. Root cause = a cross-test data-collision exposed only by running the WHOLE suite twice on one shared DB — `Pnd30_finalize_is_immutable` (or a sibling that shares its ภ.พ.30 period/filing row) seeds a FIXED period instead of `TestIds.FuturePeriod()`, so the 2nd full pass hits an already-finalized period. Surfaced now because this sprint ran the rigorous full-suite-2× gate. | **RESOLVED (main agent)** — the test ALREADY used `TestIds.FuturePeriod()`, but that helper drew from only ~99 distinct periods (`12 + Random(1,100)` months); on a PERSISTENT `teas_test` the finalized periods accumulate, so a re-run hit an already-locked one (~1/99 → grows by birthday-paradox). Two-layer fix: (1) widened `FuturePeriod` spread to `12 + Random(1,1000)` (~80 yr, unit test still green 6/6); (2) `Pnd30_finalize_is_immutable` now DELETEs any prior PND30 row for the chosen period before finalizing, so the "first finalize succeeds" precondition is deterministic. Full suite now **174/174 ×3 consecutive** on teas_test. |
| BP-08 | 🟠 | G | `ap_clerk` PV create on the live dev stack (:5080) — **PRE-EXISTING env/seed drift, NOT a Phase G regression** | E2E: `ap_clerk` POST `/payment-vouchers/` with an `admin`-resolved SVC `expenseCategoryId` (id 6) → **422 `pv.expense_category_missing` "Expense category 6 not found."** Confirmed pre-existing because the untouched `payment-voucher-non-super-rbac.spec.ts` (Sprint 7-half) **fails identically** at the same line (PV create 201 assertion) on this same stack. | `PaymentVoucherService.CreateDraftAsync` resolves the category via the tenant-filtered `_db.ExpenseCategories` (company global query filter). Hypothesis: `admin` (super) `GET /expense-categories` returns a category id (6) that `ap_clerk`'s tenant company cannot see → filter rejects it. The vendor lookup in the same request succeeds, so it is specifically the expense-category scoping that diverges between the super list and the ap_clerk write session. | **NOT A BUG — working as designed (verified by main agent).** `ExpenseCategory` IS `ITenantOwned` with `CompanyId` — its own doc comment says *"Per-company expense category"*; the global query filter (`AccountingDbContext:136`) correctly scopes it by company (§4.7). A super-admin `GET /expense-categories` returns categories ACROSS companies; picking one (id 6) that belongs to a different company than `ap_clerk`'s, then having `ap_clerk` create a PV with it, is correctly rejected. **Weakening that filter would be a §4.7/§10 multi-tenant compliance VIOLATION — must NOT be done.** Correct fix is TEST-SIDE: the acting role must pick a category in its OWN company (the pre-existing `payment-voucher-non-super-rbac.spec.ts` has the same test-data bug — Sales/test track). `purchase-chain.spec.ts` already routes around correctly (PV created as the company-scoped role). |
| BP-10 | 🟠 | G | Sales E2E `quotation-chain-flow.spec.ts` — **PRE-EXISTING, NOT Phase G** | The canonical Sales chain spec FAILS at its first step: after `/quotations/new` → fill line → click "ออกใบเสนอราคา" (Issue) and `waitForURL(/quotations/\d+/)`, `getByTestId('q-status')` never shows `Sent\|ส่งแล้ว` (15s timeout, "element(s) not found"). | Phase G added ONLY `e2e/purchase-chain.spec.ts` + doc edits — no Sales page/component/helper was touched, so this is environment/data drift on the live dev stack (same class as BP-08). root cause now CONFIRMED (main agent grep). | **DIAGNOSED — Sales test-infra gap, NOT a Purchase issue / NOT a business bug.** The Sales detail pages (`quotations/[id]`, `sales-orders/[id]`, `invoices/[id]`) have **no `data-testid="q-status" / "so-status" / "bn-status"`** on their `<StatusBadge>` — those hooks never existed (only the LIST pages have `*-filter-status`). The E2E specs (`quotation-chain-flow`, `billing-note-flow`) were written against detail-page status testids that were never added. Ironically only `po-status` exists — added THIS sprint by Phase D on `purchase-orders/[id]:51`. So the Issue submit works (URL changes) but `getByTestId('q-status')` finds nothing → timeout. **Exact fix (Sales track, NOT applied — out of Purchase sprint scope, Req §6):** wrap each Sales detail StatusBadge with `<span data-testid="q-status">…</span>` (mirror `po-status`). Then re-walk the Sales E2E. Flagged to Ham. |
| BP-09 | 🟠 | G | `vendor-invoices/[id]` detail page — **app gap (Phase D), surfaced by E2E** | `purchase-chain.spec.ts` "VI detail page widgets" step: VI detail renders `PurchaseDocumentChain` (✓) but has **NO `PaperDocument` and NO `PrintMenu`**. PO + PV detail pages render all three; VI renders only the chain panel. | Confirmed in source: `vendor-invoices/[id]/page.tsx` imports only `PurchaseDocumentChain` — no `PaperDocument`, no `PrintMenu`. Root cause is **BP-04**: there is no `GET /vendor-invoices/{id}/pdf` endpoint, so D3 could not wire a PrintMenu, and no PaperDocument render was added. | **RESOLVED (Flag-1, subAgent8)** — Ham approved an on-screen READ-ONLY `PaperDocument` for the VI detail page (Requirements §4.1 visual parity). Added `'vendor-invoice'` kind to `paper-doc-config.ts` (title "ใบกำกับภาษีซื้อ", seller=vendor / customer=our company via new `companyToCustomer`, no watermark) and rendered it on `vendor-invoices/[id]/page.tsx`. VI still gets **NO PrintMenu** (no `/pdf` endpoint — §4.6 — correct). Chain panel + StatusBadge retained; view-only (no editable inputs). FE tsc 0, next build 0/0. |

---

## Resolved

| ID | Sev | Phase | Area | Fix | Verified | 
|---|---|---|---|---|---|
| BP-03 | 🟠 | D | `components/doc/ChainRowPrint.tsx` per-row chain print | Changed `pdfPath` query from `?copy=1` → `?copy=true`. BE binds `bool? copy` which only parses `true`/`false`; `1` → 400 (same class as the cont.70 PrintMenu fix). Was latent because ChainRowPrint is only reached from the (Sales) DocumentChain rail. | `tsc --noEmit` 0 · `next build` 0/0 |
| BP-06 | 🟠 | F | `#SR9` — `purchase-orders/new/page.tsx` PO create form | Rewrote the 1-line stub (hardcoded `taxCodeId:1/VAT7/0.07`, single fixed line) into VI/Quotation-quality form: RHF+Zod, `<LineItemsTable enableProduct>` (multi-line + `<ProductPicker>` free-text fallback + per-line discount % + VAT-rate dropdown from `/system/info`, not hardcoded), rate→taxCode map copied from `QuotationForm`. Error toast now surfaces BE Thai ProblemDetails (`err.detail ?? err.title ?? tc('error')`) instead of generic-only. Resolves the pre-known PO `/new` watch-item. | `npx tsc --noEmit` EXIT 0 · `next build` EXIT 0 (54/54 pages; `/purchase-orders/new` 4.61 kB present). Live render NOT checked — BE down on :5080. |

---

## Pre-known watch-items (from spec / verified state — confirm not regressed)

- **PO `/new`** hardcodes `taxCodeId:1, taxCode:'VAT7', taxRate:0.07` (`purchase-orders/new/page.tsx:36`) → Phase F lift fixes this.
- **`?copy`** must be sent as `?copy=true` (string `1` → 400; endpoints bind `bool?`) — cont.70 fix; reuse in PrintMenu for PO/PV.
- **`next build` from `U:\frontend`** corrupts webpack → always native path (gotcha §39).
- **Posted docs read-only** — PaperDocument adoption must not re-enable edit fields on `status==="Posted"` (§4.2).
- **DocumentChain / PrintMenu doctype enum** — currently Sales-only (unverified); subAgent4 reads + extends (must not break existing Sales doctypes).
